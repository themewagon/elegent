import { Box, LinearProgress } from '@mui/material';

const Splash = () => {
  return (
    <Box sx={{ width: 1 }}>
      <LinearProgress color="primary" />
    </Box>
  );
};

export default Splash;
