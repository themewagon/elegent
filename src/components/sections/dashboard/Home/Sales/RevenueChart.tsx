import ReactEchart from 'components/base/ReactEchart';
import * as echarts from 'echarts';

const RevenueChart = () => {
  return <ReactEchart echarts={echarts} option={''} />;
};

export default RevenueChart;
